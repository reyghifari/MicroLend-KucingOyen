import React from "react";
import { styles } from "../styles";

export const Footer: React.FC = () => {
    return (
        <footer style={styles.footer}>
            <p>Team Kucing Oyen</p>
        </footer>
    );
};
